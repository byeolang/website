# byeol 홈페이지 Scrollytelling 작업 메모 (프롬프트)

다음 작업자가 재부팅 후에도 동일한 정보를 바로 확인할 수 있도록 현재까지 파악한 사항을 아래에 정리했다.

## 현재 구조 요약
- `index.html`은 front matter만 포함하며 실질적인 홈 뷰는 `_layouts/home.html`에 있다.
- `_layouts/home.html`에서 필요한 CSS/JS를 모두 로드하고 `div#scroll-section` 내에 `section#UprisingRocket`, `section#TakeOff` 두 개의 씬을 정의한다. Scene1 무대(`scene1-stage`, 연기 스테이지, 산·잔디 SVG 등)는 이미 구현돼 있다.
- 사이트 공통 스타일과 배경 레이어는 `assets/css/home.css`, 씬 전용 스타일은 `assets/css/scenes.css`에 분리되어 있다.
- ScrollTrigger 기반 씬 엔진은 `assets/js/scene.mjs`(추상 클래스)와 `assets/js/scenes.mjs`(실제 씬 등록)로 구성된다. 현재는 Scene1(`TakeOff`) 타임라인만 구현돼 있으며 `UprisingRocket`은 빈 씬으로 남아 있다.

## Scene1 구현 현황
- Scene1 컨셉은 "적은 문법, 빠른 이해"이며 `_layouts/home.html`의 `section#TakeOff` 내부에서 마크업이 완성돼 있다.
- 사용되는 이미지 자산: `assets/images/scenes/rocket.png`, `rocket-fire.png`, `rocket-smoke-left/right.png`, `launchpad.png`.
- Scene1 FX 모듈:
  - `assets/js/scene1-ignition-light.mjs`: WebGL(또는 Canvas)로 발사광 표현.
  - `assets/js/scene1-sparks.mjs`: Canvas 기반 점화 스파크.
- Scene1 타임라인(`assets/js/scenes.mjs`): 연기 가시성, 불꽃 스케일, 플레어 싱크, 로켓 상승 애니메이션 등을 ScrollTrigger 진행도에 맞춰 제어한다.

## 리소스 & 컨셉 가이드
- `../리소스 생성/scene_resource_spec.md`: 4개 Scene의 리소스·기술분담·화법 요약.
- `../리소스 생성/scene 별 스토리 보드.txt`: 각 Scene의 기능/핵심화면/필요 이미지/애니메이션/텍스트 배치/전환 규칙.
- `../리소스 생성/scene의 리소스 생성 규약.txt`: 자산 우선순위, 스타일 잠금 규칙, 프롬프트 초안.
- `../리소스 생성/기능 4개 설명.txt`: Scene별 카피 및 UI 카드 구성 지침.
- `../리소스 생성/scene*_컨셉아트.png`: Scene1~4 시네마틱 픽셀아트 레퍼런스.

## 구현 시 유의사항
1. 씬 추가 시 `_layouts/home.html`에 새로운 `<section id="...">`와 핀 배경 구조를 추가하고, `assets/css/scenes.css`에서 레이아웃·레이어 z-index를 정의한다.
2. 애니메이션 로직은 `assets/js/scenes.mjs`에 Scene 클래스를 추가해 ScrollTrigger에 등록한다. 필요하면 공용 유틸을 `assets/js/sceneX-*.mjs` 형태로 분리한다.
3. Scene 전환은 ScrollTrigger scrub 기반이므로 `Scene` 생성자에 넘기는 `heightRatio`를 적절히 지정해 스크롤 진행 길이를 맞춘다.
4. 기존 home 배경(`assets/js/homeBg.mjs`, `assets/js/seaBg.mjs`)에서 뷰포트/푸터 높이를 CSS 변수 `--vh`, `--vfooter`로 노출하므로 Scene CSS에서 이 값을 활용할 수 있다.
5. 픽셀아트 자산 생성 시 리소스 규약의 공통 조건(시네마틱 픽셀아트, 텍스트·UI 없음, 투명 배경, 로켓 디자인 잠금 등)을 프롬프트에 반드시 포함한다.

## 다음 단계 제안
1. Scene2용 마크업/스타일/타임라인을 추가해 구름층 돌파 구간을 구현한다. (컨셉: 빌드 벽 없는 개발)
2. 이후 Scene3, Scene4 순으로 무대·자산을 확장하고, Three.js를 활용하는 부분(Scene3 우주 파트, Scene4 행성/별자리)을 분리 모듈로 개발한다.
3. UI 카드 텍스트는 `기능 4개 설명.txt`를 그대로 사용하며, Storyboard에 정의된 위치와 등장 순서를 GSAP로 제어한다.

이 문서를 계속 갱신하면서 진행 상황과 규칙을 덧붙이면, 재부팅 이후에도 동일한 컨텍스트를 빠르게 공유할 수 있다.

## Scene2 리소스 생성 계획 (2026-03-31)

### Scene2 목표
- 기능 메시지: `빌드 벽 없는 개발`
- 장면 역할: 로켓이 구름층을 뚫고 상승해 상층 대기와 대기권 돌파를 통과한다.
- 사용자 감정: 막히지 않고 바로 반응하는 추진력, 답답함을 뚫고 치고 올라가는 속도감
- 시각 은유: 빌드 대기 없는 빠른 개발 루프를 로켓의 연속 상승과 돌파로 상징한다.

### 기본 원칙
- 기준 비율은 `16:9`
- 아트 스타일은 `시네마틱 픽셀아트`
- 텍스트, 설명, 플로우 카드, 그라데이션 백플레이트는 이미지에 박지 않고 `DOM UI`로 구현
- 로켓은 Scene1 디자인을 유지한 `2.5D 스프라이트` 중심으로 간다
- Scene2는 `이미지 + GSAP` 중심, 필요하면 얕은 깊이감만 보조적으로 추가한다
- 생성 리소스는 장면 전체 일러스트보다 `레이어 자산` 위주로 만든다

### 생성 대상
- `rocket_ascent_master`: Scene2 상승용 기준 로켓 스프라이트
- `cloud_layer_far`: 원경 구름 레이어
- `cloud_layer_mid`: 중경 메인 구름 레이어
- `cloud_layer_front`: 전경 구름 레이어
- `upper_atmosphere_backdrop`: 밝은 하늘에서 남색 상층 대기로 넘어가는 배경
- `engine_flame_boost`: Scene1보다 더 강한 추진 불꽃
- `mist_cloud_soft`: 구름 사이를 메우는 미스트/안개 레이어
- `atmosphere_break_ring`: 대기권 돌파 순간의 빛 링 또는 충격파
- `speed_trail_soft`: 로켓 후방의 속도 잔광

### 생성하지 않을 것
- 타이틀 / 설명 텍스트
- 일반 개발 vs byeol 플로우 비교 카드
- 검정 → 투명 그라데이션 백플레이트
- 단순 반복 속도선 패턴
- 스크롤 전환 애니메이션
- 장면 전체가 하나로 붙은 완성 일러스트

### 생성 우선순위
1. `rocket_ascent_master`
2. `cloud_layer_far`
3. `cloud_layer_mid`
4. `cloud_layer_front`
5. `upper_atmosphere_backdrop`
6. `engine_flame_boost`
7. `mist_cloud_soft`
8. `atmosphere_break_ring`
9. `speed_trail_soft`

### 최소 세트
아래 5개만 먼저 생성하면 Scene2 기본 합성 테스트가 가능하다.

- `rocket_ascent_master`
- `cloud_layer_far`
- `cloud_layer_mid`
- `cloud_layer_front`
- `upper_atmosphere_backdrop`

### 스타일 잠금 규칙
모든 프롬프트 공통 조건:

- 시네마틱 픽셀아트
- 너무 저해상도 레트로풍 말고, 현대적인 디테일이 있는 픽셀아트
- 텍스트 없음
- UI 없음
- 테두리 없음
- 워터마크 없음
- 배경은 없거나 분리 가능한 독립 레이어 형태
- 게임 리소스로 잘라 쓰기 좋은 선명한 실루엣

로켓 공통 잠금 규칙:

- 흰색 본체
- 빨간 세로 스트라이프
- 검은 조종석 창
- 너무 현실적인 NASA 스타일 말고, 친숙하고 게임적인 실루엣
- Scene1 로켓과 같은 디자인 계열

Scene2 전용 분위기 규칙:

- 구름은 귀여운 솜사탕풍보다 속도감 있는 대형 질량으로 표현
- 하늘은 밝은 청색에서 깊은 남색으로 자연스럽게 넘어가야 함
- 전체 톤은 Scene1보다 더 차갑고, 더 높은 고도 느낌을 줘야 함

### 검수 기준
- Scene1 로켓과 디자인 연속성이 있는가
- 구름 3레이어를 쌓았을 때 깊이감이 생기는가
- 좌측 텍스트/UI를 위한 공간이 남는가
- 로켓 진행 방향과 카드 배치가 충돌하지 않는가
- 하늘에서 상층 대기로 자연스럽게 넘어가는가
- 대기권 돌파 연출이 과하지 않으면서도 읽히는가
- `빌드 벽 없음`이 정지된 풍경이 아니라 추진력으로 느껴지는가

## Scene2 자산별 프롬프트 초안

### 1. `rocket_ascent_master`
- 분류: 공용 로켓 파생 자산
- 표현: `2.5D`
- 용도: Scene2에서 구름층을 통과하며 상승하는 기준 로켓

프롬프트 초안

시네마틱 픽셀아트 스타일의 중형 우주 로켓 단독 오브젝트, Scene2 상승용 기준 디자인, 위쪽으로 빠르게 상승하는 자세, 오른쪽 위 방향으로 기울어진 3/4 뷰, 흰색 본체, 빨간 세로 스트라이프, 검은 조종석 창, Scene1 로켓과 같은 디자인 계열, 너무 현실적인 NASA 스타일 말고 친숙하고 게임적인 실루엣, 날렵하지만 과하게 복잡하지 않은 구조, 구름층을 뚫고 올라가는 추진력이 느껴지는 포즈, 선명한 외곽선, 약간의 금속성 음영, 현대적인 고해상도 픽셀아트 감성, 게임 리소스로 잘라 쓰기 좋은 오브젝트, 배경 없음, 투명 배경, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 2. `cloud_layer_far`
- 분류: Scene2 배경 자산
- 표현: `2D`
- 용도: 원경 하늘 쪽 구름 레이어

프롬프트 초안

시네마틱 픽셀아트 스타일의 원경 구름 레이어, 넓고 얇게 퍼진 상공의 구름띠, 밝은 청색 하늘 위에 자연스럽게 겹쳐지는 대형 구름 질량, 너무 귀엽거나 둥글기만 한 구름이 아니라 속도감 있는 넓은 덩어리감, 부드러운 흰색과 옅은 회청색 음영, 화면 여러 곳에 나뉘어 배치해도 자연스러운 구조, 장면 뒤쪽에 깔리는 배경 레이어 용도, 선명하지만 과하게 복잡하지 않은 픽셀아트, 배경 없음 또는 투명 배경 전제, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 3. `cloud_layer_mid`
- 분류: Scene2 배경 자산
- 표현: `2D`
- 용도: Scene2에서 가장 존재감이 큰 메인 구름층

프롬프트 초안

시네마틱 픽셀아트 스타일의 중경 메인 구름 레이어, 로켓이 관통하며 지나갈 수 있는 밀도 있는 대형 구름층, 화면 중단을 가로지르는 넓은 구름 질량, 강한 햇빛을 받은 밝은 흰색과 차가운 회청색 그림자가 함께 보이는 형태, 부드럽지만 무게감 있는 실루엣, 속도감 있는 장면에 어울리는 시네마틱 질량감, 여러 조각으로 분해해 겹쳐 써도 자연스러운 구조, 배경 없음 또는 투명 배경 전제, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 4. `cloud_layer_front`
- 분류: Scene2 전경 자산
- 표현: `2D`
- 용도: 카메라 앞을 스치며 깊이감과 속도감을 주는 전경 구름

프롬프트 초안

시네마틱 픽셀아트 스타일의 전경 구름 레이어, 카메라 바로 앞을 스쳐 지나가는 커다란 구름 덩어리, 화면 가장자리나 하단을 크게 차지할 수 있는 과감한 실루엣, 밝은 흰색 중심에 차가운 그림자와 약한 블룸 느낌, 로켓 상승 장면의 전경 패럴랙스용으로 쓰기 좋은 형태, 지나치게 동글동글한 만화풍보다 넓고 빠르게 스치는 덩어리감, 일부가 프레임 밖으로 잘려도 자연스러운 구성, 배경 없음 또는 투명 배경 전제, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 5. `upper_atmosphere_backdrop`
- 분류: Scene2 배경 자산
- 표현: `2D`
- 용도: 밝은 하늘에서 남색 상층 대기로 넘어가는 전환 배경

프롬프트 초안

시네마틱 픽셀아트 스타일의 상층 대기 배경 레이어, 아래는 밝은 청색 하늘이고 위로 갈수록 깊은 남색으로 자연스럽게 이어지는 고고도 하늘, 아주 옅은 대기층 결이나 빛 번짐이 느껴지는 구조, 구름과 로켓을 따로 얹기 좋은 깔끔한 배경, Scene2의 상승 후반과 대기권 돌파 직전 분위기를 만드는 용도, 과도한 오브젝트 없이 색과 공기감이 중심인 시네마틱 픽셀아트, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 6. `engine_flame_boost`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: Scene1보다 더 강하게 뻗는 상승용 추진 불꽃

프롬프트 초안

시네마틱 픽셀아트 스타일의 로켓 상승용 강한 엔진 불꽃 단독 오브젝트, 아래 방향으로 길고 강하게 뻗는 추진 화염, 중심은 흰색과 노란색으로 매우 밝고 바깥은 주황색과 붉은색이 길게 번지는 형태, Scene1 점화 불꽃보다 더 길고 더 빠른 추진력이 느껴지는 실루엣, 하늘과 구름 배경 위에 얹기 좋은 선명한 픽셀아트 FX, 배경 없음, 투명 배경, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 7. `mist_cloud_soft`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: 구름층 사이를 통과할 때 얹는 미스트/안개

프롬프트 초안

시네마틱 픽셀아트 스타일의 부드러운 안개와 미스트 레이어, 구름층 사이를 스치듯 지나가는 얇고 넓은 흰색 수증기, 완전히 불투명한 구름 덩어리보다 훨씬 가볍고 얇은 형태, 로켓이나 구름 위에 겹쳐서 속도감과 공기감을 보강하기 좋은 레이어, 가장자리가 부드럽게 풀리는 실루엣, 배경 없음, 투명 배경, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 8. `atmosphere_break_ring`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: 대기권 돌파 순간의 링/충격파 효과

프롬프트 초안

시네마틱 픽셀아트 스타일의 대기권 돌파용 빛 링 이펙트 단독 오브젝트, 얇고 밝은 원형 충격파 또는 타원형 에너지 링, 중심은 매우 밝은 흰색과 옅은 하늘색, 가장자리는 옅은 청색 광채로 번지는 형태, 로켓이 상층 대기를 뚫는 순간을 강조하는 연출용, 너무 SF HUD처럼 인공적인 UI 느낌 말고 자연광과 충격파의 중간 같은 표현, 배경 없음, 투명 배경, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음

### 9. `speed_trail_soft`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: 로켓 후방의 고속 잔광/속도 흔적

프롬프트 초안

시네마틱 픽셀아트 스타일의 로켓 고속 상승용 잔광 흔적 단독 오브젝트, 길고 부드럽게 늘어지는 밝은 속도 잔상, 중심은 흰색에 가깝고 바깥으로 갈수록 옅은 하늘색과 투명한 광채로 사라지는 형태, 구름층 통과 장면에서 빠른 상승을 보강하는 용도, 불꽃 그 자체보다는 빛과 잔상에 가까운 실루엣, 배경 없음, 투명 배경, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음


## Scene2 최종 이미지 생성 프롬프트 (2026-03-31 최종본)

이 섹션은 위의 `프롬프트 초안`과 `프롬프트 보정 메모`를 실제 생성용으로 다시 다듬은 **우선 사용본**이다.
다음 재부팅 이후에도 Scene2 자산 생성이 필요하면, 먼저 이 섹션의 프롬프트를 그대로 사용하면 된다.

핵심 원칙은 다시 한 번 아래와 같다.

- Scene2는 **Scene1 컨셉아트의 복제**가 아니라 **Scene1 실제 구현 자산의 확장**이다.
- 완성 포스터가 아니라 **웹 스크롤 애니메이션 합성용 개별 레이어 자산**을 만든다.
- 로켓은 반드시 **현재 Scene1 로켓 스프라이트의 직계 파생형**처럼 보여야 한다.
- 구름은 귀엽고 둥근 만화 구름보다 **빠르게 스치는 대형 질량**이어야 한다.
- 배경은 텍스트/UI 카드와 공존해야 하므로, **과밀한 일러스트가 아니라 비워진 공간이 있는 하늘 레이어**여야 한다.

### Scene2 공통 전달 문장

아래 문장은 Scene2 자산 생성 시 공통으로 먼저 붙인다.

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다.
```

### 로켓 계열 공통 잠금 문장

`rocket_ascent_master`, 필요시 로켓 파생 자산 생성 시 아래 문장을 함께 붙인다.

```text
현재 Scene1에서 사용 중인 로켓 스프라이트와 같은 디자인 계열이어야 한다. 새로운 우주선 디자인을 발명하지 말고, 흰색 본체, 빨간 세로 스트라이프, 검은 조종석 창, 또렷한 외곽선, 친숙하고 게임적인 중형 로켓 비율을 유지해야 한다. 너무 현실적인 NASA 스타일, 과도한 기계 디테일, 지나치게 길거나 복잡한 실루엣은 금지한다.
```

### 구름 계열 공통 잠금 문장

`cloud_layer_far`, `cloud_layer_mid`, `cloud_layer_front`, `mist_cloud_soft` 생성 시 아래 문장을 함께 붙인다.

```text
귀엽고 둥근 솜사탕형 구름보다, 빠르게 스치는 대형 질량의 구름이어야 한다. 웹 애니메이션에서 여러 장 겹쳐 쓸 수 있도록 분리 가능한 덩어리 구조여야 하며, 중앙 텍스트/UI 공간을 침범하지 않도록 과밀한 구성을 피해야 한다. Scene1 실제 연기 자산과도 어울리도록 약간의 볼륨감과 공기감은 있지만, 과도한 풍경화처럼 되면 안 된다.
```

### 배경 계열 공통 잠금 문장

`upper_atmosphere_backdrop` 생성 시 아래 문장을 함께 붙인다.

```text
배경은 자체로 완결된 일러스트가 아니라 로켓과 구름을 따로 얹기 위한 하늘 레이어다. Scene1 실제 구현처럼 UI 카드와 텍스트가 올라갈 공간을 남겨야 하므로, 복잡한 오브젝트나 과도한 구름, 행성, 별, SF 장식을 넣지 않는다.
```

### 포스터풍으로 흐를 때 추가 보정 문장

이미지 생성 모델이 자꾸 완성 일러스트나 포스터처럼 그리면 아래 문장을 추가한다.

```text
웹사이트용 최종 장면 일러스트를 그리는 것이 아니라, 애니메이션 합성에 사용할 개별 레이어 자산 하나만 만들어야 한다. 한 이미지에는 핵심 오브젝트 하나만 담고, 다른 요소는 최대한 제거해 달라.
```

## Scene2 자산별 최종 프롬프트

### 1. `rocket_ascent_master`
- 분류: 공용 로켓 파생 자산
- 표현: `2.5D`
- 용도: Scene2에서 구름층을 뚫고 상승하는 기준 로켓

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 현재 Scene1에서 사용 중인 로켓 스프라이트와 같은 디자인 계열이어야 한다. 새로운 우주선 디자인을 발명하지 말고, 흰색 본체, 빨간 세로 스트라이프, 검은 조종석 창, 또렷한 외곽선, 친숙하고 게임적인 중형 로켓 비율을 유지해야 한다. 너무 현실적인 NASA 스타일, 과도한 기계 디테일, 지나치게 길거나 복잡한 실루엣은 금지한다. 시네마틱 픽셀아트 스타일의 중형 우주 로켓 단독 오브젝트, Scene2 상승용 기준 디자인, 오른쪽 위 방향으로 빠르게 상승하는 자세, 상승 중인 역동적인 3/4 뷰, 하단 엔진 노즐이 읽히는 구조, Scene1 로켓보다 자세만 더 공격적으로 바뀐 파생형, 약간의 금속성 음영, 깨끗하고 또렷한 외곽선, 웹 합성용으로 잘 잘리는 스프라이트 비율, 로켓 단독만 표현, 불꽃과 연기와 배경은 포함하지 않음, 투명 배경
```

### 2. `cloud_layer_far`
- 분류: Scene2 배경 자산
- 표현: `2D`
- 용도: 원경 하늘 쪽 구름 레이어

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 귀엽고 둥근 솜사탕형 구름보다, 빠르게 스치는 대형 질량의 구름이어야 한다. 웹 애니메이션에서 여러 장 겹쳐 쓸 수 있도록 분리 가능한 덩어리 구조여야 하며, 중앙 텍스트/UI 공간을 침범하지 않도록 과밀한 구성을 피해야 한다. Scene1 실제 연기 자산과도 어울리도록 약간의 볼륨감과 공기감은 있지만, 과도한 풍경화처럼 되면 안 된다. 시네마틱 픽셀아트 스타일의 원경 구름 레이어, 넓고 얇게 퍼진 상공의 구름띠, 밝은 청색 하늘 위에서 멀리 깔리는 구름 질량, 너무 두껍지 않고 시야를 막지 않는 구조, 여러 위치에 나눠 배치하기 좋은 느슨한 덩어리 분포, 흰색과 옅은 회청색 음영, 하늘이 충분히 비쳐 보이는 원경용 레이어, 배경 없음 또는 투명 배경 전제
```

### 3. `cloud_layer_mid`
- 분류: Scene2 배경 자산
- 표현: `2D`
- 용도: Scene2의 핵심 메인 구름층

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 귀엽고 둥근 솜사탕형 구름보다, 빠르게 스치는 대형 질량의 구름이어야 한다. 웹 애니메이션에서 여러 장 겹쳐 쓸 수 있도록 분리 가능한 덩어리 구조여야 하며, 중앙 텍스트/UI 공간을 침범하지 않도록 과밀한 구성을 피해야 한다. Scene1 실제 연기 자산과도 어울리도록 약간의 볼륨감과 공기감은 있지만, 과도한 풍경화처럼 되면 안 된다. 시네마틱 픽셀아트 스타일의 중경 메인 구름 레이어, 로켓이 실제로 관통해 지나갈 수 있을 것 같은 밀도 있는 대형 구름층, 화면 중단을 가로지르되 하나의 닫힌 풍경화처럼 붙지 않고 여러 조각으로 분해 가능한 구조, 강한 햇빛을 받은 밝은 흰색과 차가운 회청색 그림자, 부드럽지만 무게감 있는 실루엣, Scene2에서 가장 존재감이 큰 구름 질량, 배경 없음 또는 투명 배경 전제
```

### 4. `cloud_layer_front`
- 분류: Scene2 전경 자산
- 표현: `2D`
- 용도: 카메라 앞을 스치며 깊이감과 속도감을 주는 전경 구름

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 귀엽고 둥근 솜사탕형 구름보다, 빠르게 스치는 대형 질량의 구름이어야 한다. 웹 애니메이션에서 여러 장 겹쳐 쓸 수 있도록 분리 가능한 덩어리 구조여야 하며, 중앙 텍스트/UI 공간을 침범하지 않도록 과밀한 구성을 피해야 한다. Scene1 실제 연기 자산과도 어울리도록 약간의 볼륨감과 공기감은 있지만, 과도한 풍경화처럼 되면 안 된다. 시네마틱 픽셀아트 스타일의 전경 구름 레이어, 카메라 바로 앞을 크게 스쳐 지나가는 대형 구름 덩어리, 화면 가장자리나 하단에 과감하게 걸치는 실루엣, 일부가 프레임 밖으로 잘려도 자연스러운 구성, 중앙 로켓과 UI 공간은 지나치게 막지 않는 배치, 밝은 흰색 중심에 차가운 그림자와 약한 블룸 느낌, 패럴랙스 전경용 레이어, 배경 없음 또는 투명 배경 전제
```

### 5. `upper_atmosphere_backdrop`
- 분류: Scene2 배경 자산
- 표현: `2D`
- 용도: 밝은 하늘에서 남색 상층 대기로 넘어가는 전환 배경

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 배경은 자체로 완결된 일러스트가 아니라 로켓과 구름을 따로 얹기 위한 하늘 레이어다. Scene1 실제 구현처럼 UI 카드와 텍스트가 올라갈 공간을 남겨야 하므로, 복잡한 오브젝트나 과도한 구름, 행성, 별, SF 장식을 넣지 않는다. 시네마틱 픽셀아트 스타일의 상층 대기 배경 레이어, 아래쪽은 밝은 청색 하늘이고 위로 갈수록 깊은 남색으로 자연스럽게 이어지는 고고도 하늘, 매우 옅은 대기층 결이나 빛 번짐만 있는 깔끔한 구조, 구름과 로켓을 얹기 위한 비워진 공간이 충분한 배경, Scene1 하늘과 자연스럽게 이어지되 Scene2 후반으로 갈수록 차갑고 높은 고도감이 느껴지는 색 전환, 별과 행성과 번개와 과도한 오브젝트 없음
```

### 6. `engine_flame_boost`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: Scene1보다 더 강하게 뻗는 상승용 추진 불꽃

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 시네마틱 픽셀아트 스타일의 로켓 상승용 강한 엔진 불꽃 단독 오브젝트, Scene1 불꽃과 같은 색 언어를 유지한 고출력 버전, 중심은 거의 흰색과 밝은 노란색, 바깥은 주황색과 붉은색이 길게 번지는 구조, 엔진 노즐 아래에서 좁게 시작해 아래로 길고 강하게 뻗는 추진 화염, 복잡하게 갈라지는 화염보다는 중심 추진력이 명확한 실루엣, Scene1보다 더 빠르고 더 강한 상승을 느끼게 하는 형태, 배경 없음, 투명 배경
```

### 7. `mist_cloud_soft`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: 구름층 사이를 통과할 때 얹는 미스트/안개

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 귀엽고 둥근 솜사탕형 구름보다, 빠르게 스치는 대형 질량의 구름이어야 한다. 웹 애니메이션에서 여러 장 겹쳐 쓸 수 있도록 분리 가능한 덩어리 구조여야 하며, 중앙 텍스트/UI 공간을 침범하지 않도록 과밀한 구성을 피해야 한다. Scene1 실제 연기 자산과도 어울리도록 약간의 볼륨감과 공기감은 있지만, 과도한 풍경화처럼 되면 안 된다. 시네마틱 픽셀아트 스타일의 부드러운 미스트와 얇은 안개 레이어, 완전한 구름 덩어리보다 훨씬 얇고 가벼운 흰색 수증기, 가장자리가 부드럽게 풀리는 실루엣, 로켓과 구름 위에 얹었을 때 공기감과 속도감만 살려주는 보조 자산, 단독으로 화려한 그림이 아니라 합성용 FX 레이어, 배경 없음, 투명 배경
```

### 8. `atmosphere_break_ring`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: 대기권 돌파 순간의 링/충격파 효과

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 시네마틱 픽셀아트 스타일의 대기권 돌파용 빛 링 이펙트 단독 오브젝트, 매우 얇고 밝은 원형 또는 타원형 충격파 링, 중심은 매우 밝은 흰색과 옅은 하늘색, 가장자리는 은은한 청색 광채로만 번지는 형태, SF HUD나 인터페이스 원형 UI처럼 보이지 않게 자연광과 충격파의 중간 같은 느낌, 패턴이나 문자나 눈금 없이 단순한 에너지 링, 배경 없음, 투명 배경
```

### 9. `speed_trail_soft`
- 분류: Scene2 FX 자산
- 표현: `2D FX`
- 용도: 로켓 후방의 고속 잔광/속도 흔적

```text
Scene2 스크롤텔링용 웹 애니메이션 합성 자산을 생성하는 작업이다. 완성 포스터나 장면 전체 일러스트를 그리는 것이 아니라, 분리해서 겹쳐 쓸 수 있는 개별 레이어 자산을 만들어야 한다. 시네마틱 픽셀아트 스타일, 너무 저해상도 레트로풍 말고 현대적인 디테일이 있는 픽셀아트, 게임 스프라이트처럼 선명한 실루엣, 텍스트 없음, UI 없음, 테두리 없음, 워터마크 없음. Scene1 실제 구현 자산과 이어져야 하므로 지나치게 회화적인 포스터풍이나 SF 콘셉트 아트풍으로 가지 말고, 웹에서 합성하기 좋은 또렷한 자산으로 만들어야 한다. 시네마틱 픽셀아트 스타일의 로켓 고속 상승용 잔광 흔적 단독 오브젝트, 불꽃 그 자체보다 더 흐리고 더 빛에 가까운 길고 부드러운 속도 잔상, 중심은 흰색에 가깝고 바깥으로 갈수록 옅은 하늘색과 거의 투명한 광채로 사라지는 형태, 엔진 불꽃을 보조하는 자산으로서 과도한 존재감을 가지지 않음, 빠른 상승의 흔적만 읽히는 심플한 실루엣, 배경 없음, 투명 배경
```

### 최종 사용 순서

실제 이미지 생성 시에는 아래 순서를 권장한다.

1. `rocket_ascent_master`
2. `cloud_layer_far`
3. `cloud_layer_mid`
4. `cloud_layer_front`
5. `upper_atmosphere_backdrop`
6. 합성 테스트
7. 부족하면 `engine_flame_boost`, `mist_cloud_soft`, `atmosphere_break_ring`, `speed_trail_soft` 순서로 추가

### 최종 판단 메모

- Scene2는 반드시 **Scene1 실제 구현 자산과 이어져 보여야 한다.**
- 자산 하나하나가 예쁜 그림인지보다, **웹에서 겹쳤을 때 잘 작동하는지**가 더 중요하다.
- 특히 로켓은 새 디자인 금지, 구름은 포스터풍 금지, 배경은 과밀 금지, 대기권 링은 HUD풍 금지로 기억하면 된다.
